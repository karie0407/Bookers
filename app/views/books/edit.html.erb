<h1>Editing Book</h1>
<%= form_with model: @bookook. do |f| %>
 <div class="text-success">
  <%= flash[:notice] %>
</div>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
<% end %>